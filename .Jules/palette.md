## 2025-10-26 - Missing ARIA Labels on Forms and Buttons
**Learning:** The application relies heavily on visual cues (placeholders, icons) for form inputs and buttons, completely omitting accessible names for screen reader users.
**Action:** Always check for and add `aria-label` to inputs without visible labels and icon-only buttons during the "Observe" phase.
